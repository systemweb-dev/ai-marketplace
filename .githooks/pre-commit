#!/bin/sh
# Gate de segurança — bloqueia commits com credenciais ou dados pessoais.
# Instale com:  make hooks   (configura core.hooksPath=.githooks)
# Pular (use com MUITO cuidado):  git commit --no-verify
root="$(git rev-parse --show-toplevel)"
exec python3 "$root/scripts/scan_secrets.py" staged
