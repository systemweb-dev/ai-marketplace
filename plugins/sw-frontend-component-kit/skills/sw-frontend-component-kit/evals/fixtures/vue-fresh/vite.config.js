import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// projeto Vue 3 recém-criado — ainda sem design system nem componentes de UI
export default defineConfig({
  plugins: [vue()],
})
